import unittest
from first import three_way_split

class test_suite(unittest.TestCase):
    '''tests to ensure proper three way splitting of function three_way_split'''    def test_split(self):
        self.assert X_val, X_test, X_train, y_test, y_train, y_val = True

